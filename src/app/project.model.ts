export class Project {
  constructor(public title: string, public owner: string, public description: string, public mission: string, public goal: number, public funding: number, public rewards: string[]) { }
}
